ENV["SINATRA_ENV"] ||= "development"

require_relative './config/environment' #file to load up our environment
require 'sinatra/activerecord/rake' #to get Rake tasks from the sinatra-activerecord gem


#rake gives us the ability to quickly make files and setup automated tasks.
